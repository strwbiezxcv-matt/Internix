# Dependencies & runtime
node_modules/
data/*
*.db
*.db-journal
*.db-wal
# The production DB snapshot is intentionally bundled so serverless
# deployments (Vercel) cold-start with the full company/program/opportunity
# catalogue. Do not remove this exception.
!data/internconnect.db

# Secrets
.env
!/.env.example

# Logs & OS
*.log
.DS_Store
Thumbs.db